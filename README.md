***NOS(Niel Operating System)***

Nos containes a bootloader NBL, writen in asm and a kernel writen in C.
 **The bootloader**

As said is the bootloader writen in asm and can be campiled using nasm. It uses a header for discovering kernels(bootable programs) in a custom format that decleares a name a startaddress and the size in sectors.
